/// <reference types="node" />
import { FaaSConfig, Env } from './config';
import { IFluenceAppService } from './IFluenceAppService';
import { JSONArray, JSONObject } from './types';
export declare const defaultNames: {
    avm: {
        file: string;
        package: string;
    };
    marine: {
        file: string;
        package: string;
    };
    workerScriptPath: {
        web: string;
        node: string;
    };
};
export declare class FluenceAppService implements IFluenceAppService {
    private _worker?;
    private _workerPath;
    constructor(workerScriptPath?: string);
    init(controlModule: SharedArrayBuffer | Buffer): Promise<void>;
    createService(serviceModule: SharedArrayBuffer | Buffer, serviceId: string, faaSConfig?: FaaSConfig, envs?: Env): Promise<void>;
    callService(serviceId: string, functionName: string, args: JSONArray | JSONObject, callParams: any): Promise<unknown>;
    terminate(): Promise<void>;
}
