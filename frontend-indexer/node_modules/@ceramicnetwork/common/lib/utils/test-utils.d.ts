import { CID } from 'multiformats/cid';
import { StreamID } from '@ceramicnetwork/streamid';
import type { StreamState, Stream } from '../stream.js';
import { RunningStateLike } from '../running-state-like.js';
import type { CeramicApi } from '../ceramic-api.js';
export declare class TestUtils {
    static waitForConditionOrTimeout(predicate: () => Promise<boolean>, timeoutMs?: number): Promise<boolean>;
    static waitForState(stream: Stream, timeoutMs: number, predicate: (state: StreamState) => boolean, onFailure: (state: StreamState) => void): Promise<void>;
    static waitForAnchor(stream: Stream, timeout: number): Promise<void>;
    static runningState(state: StreamState): RunningStateLike;
    static delay(ms: number): Promise<void>;
    static isPinned(ceramic: CeramicApi, streamId: StreamID): Promise<boolean>;
    static randomCID(): CID;
    static anchorUpdate(ceramic: CeramicApi, stream: Stream): Promise<void>;
}
//# sourceMappingURL=test-utils.d.ts.map